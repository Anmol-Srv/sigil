name: sigil
version: 0.1.0
description: >
  Persistent local memory via a Sigil install. Recall reads a per-platform
  namespace plus `default`; durable writes occur only through the explicit
  sigil_remember tool.
hooks:
  - prefetch
  - sync_turn
